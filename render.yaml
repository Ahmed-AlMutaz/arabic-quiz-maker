# Render.com Free Deployment Configuration
services:
  - type: web
    name: arabic-quiz-maker
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: ENVIRONMENT
        value: production
      - key: PORT
        value: 7860
    healthCheckPath: /health
